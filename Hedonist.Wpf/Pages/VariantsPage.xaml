<Page x:Class="Hedonist.Wpf.Pages.VariantsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Hedonist.Wpf.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Variants">

    <Grid Width="1080" VerticalAlignment="Center">
        <StackPanel Grid.Row="1">
            <Grid Height="320" Background="Black" Margin="0">
                <Grid.Resources >
                    <Style TargetType="Border" >
                        <Setter Property="Padding" Value="0" />
                    </Style>
                </Grid.Resources>
                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Left"
                    Width="934" Height="196" TextWrapping="Wrap"
                    FontFamily="Montserrat" Margin="53 0 0 0" Padding="0"
                    Foreground="#FFFFFF"
                    FontSize="50 pt" FontWeight="ExtraBlack" FontStyle="Normal"
                    Text="От чего получаешь удовольствие ты?"/>
            </Grid>
            <Button Tag="Images\Art.png" Style="{StaticResource variantsButton}"></Button>
            <Button Tag="Images\Music.png" Style="{StaticResource variantsButton}"></Button>
            <Button Tag="Images\Trends.png" Style="{StaticResource variantsButton}"></Button>
            <Button Tag="Images\Food.png" Style="{StaticResource variantsButton}"></Button>
            <Button Tag="Images\Movement.png" Style="{StaticResource variantsButton}"></Button>
            <Button Tag="Images\Mixology.png" Style="{StaticResource variantsButton}"></Button>
        </StackPanel>
    </Grid>
</Page>
